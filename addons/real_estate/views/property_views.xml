<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- List View for Properties -->
    <record id="view_property_list" model="ir.ui.view">
        <field name="name">real.estate.property.list</field>
        <field name="model">real.estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties List">
                <field name="name" string="Name"/>
                <field name="price" string="Price"/>
                <field name="state" string="State"/>
                <field name="partner_id" string="Partner"/>
            </list>
        </field>
    </record>

    <!-- Form View for Properties -->
    <record id="view_property_form" model="ir.ui.view">
        <field name="name">real.estate.property.form</field>
        <field name="model">real.estate.property</field>
        <field name="arch" type="xml">
            <form string="Property Form">
                <sheet>
                    <group>
                        <field name="name" string="Name"/>
                        <field name="price" string="Price"/>
                        <field name="state" string="State"/>
                        <field name="partner_id" string="Partner"/>
                        <field name="description" string="Description"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action to Open Properties in List + Form View -->
    <record id="action_real_estate_properties" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">real.estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to create a new property.
            </p>
        </field>
    </record>

    <!-- Menus -->
    <menuitem id="menu_real_estate_root" name="Real Estate"/>
    <menuitem id="menu_real_estate_properties" name="Properties" parent="menu_real_estate_root" action="action_real_estate_properties"/>

</odoo>
